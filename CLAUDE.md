# План Разработки Системы HerZog v3.0 ("Политический Инструмент")

## Архитектурный Обзор

Система HerZog - это инструмент для быстрого формирования управляемого календарного плана строительных работ на основе сметных данных. Система работает как конвейер обработки данных через 8 этапов с использованием AI-агентов.

### Основные Принципы
- Инкрементальная разработка
- Модульная архитектура 
- Гибкость для бизнес-требований заказчика
- Полное логирование всех этапов
- Промты хранятся отдельно в папке prompts/

## Детальный План Разработки

### Этап 1: Создание Базовой Архитектуры 🏗️

#### 1.1 Структура Директорий
```
/Herzog_v3/
├── .env                          # API ключи и токены
├── main_bot.py                   # Точка входа - Телеграм бот
├── /src/                         # Исходный код
│   ├── main_pipeline.py          # Главный дирижер конвейера
│   ├── /telegram_bot/            # Логика телеграм-бота
│   │   ├── __init__.py
│   │   ├── handlers.py           # Обработчики команд и сообщений
│   │   └── questionnaire.py      # Пошаговый опрос пользователя
│   ├── /data_processing/         # Модули обработки данных
│   │   ├── __init__.py
│   │   ├── extractor.py          # Шаг 1: Парсер Excel
│   │   ├── classifier.py         # Шаг 2: Классификатор
│   │   ├── preparer.py           # Шаг 3: Подготовка единого файла
│   │   └── reporter.py           # Шаг 8: Excel-отчет
│   ├── /ai_agents/               # AI агенты (шаги 4-7)
│   │   ├── __init__.py
│   │   ├── agent_1_conceptualizer.py
│   │   ├── agent_2_strategist.py
│   │   ├── agent_3_accountant.py
│   │   └── agent_4_foreman.py
│   ├── /shared/                  # Общие утилиты
│   │   ├── __init__.py
│   │   ├── timeline_blocks.py
│   │   └── utils.py
│   └── /prompts/                 # Промты для AI агентов
│       ├── agent_1_prompt.txt
│       ├── agent_2_prompt.txt
│       ├── agent_3_prompt.txt
│       └── agent_4_prompt.txt
└── /projects/                    # Рабочие директории проектов
    └── /{user_id}/
        └── /{project_id}/
            ├── 0_input/
            ├── 1_extracted/
            ├── 2_classified/
            ├── 3_prepared/
            ├── 4_conceptualized/
            ├── 5_scheduled/
            ├── 6_accounted/
            ├── 7_staffed/
            └── 8_output/
```

### Этап 2: Телеграм-Бот и Сбор Директив 🤖

#### 2.1 Функциональность
- Прием XLSX файлов смет
- Пошаговый опрос для сбора параметров:
  - Желаемое количество строк в графике
  - Диапазон дат проекта  
  - Количество рабочих на площадке
  - Особые указания для каждого агента
- Создание структуры папок проекта
- Сохранение в `0_input/directives.json`

#### 2.2 Ключевые Файлы
- `main_bot.py` - точка входа
- `src/telegram_bot/handlers.py` - обработчики
- `src/telegram_bot/questionnaire.py` - опросник

### Этап 3: Конвейер Обработки Данных 📊

#### 3.1 Шаг 1: Экстрактор (extractor.py)
- **Вход**: `0_input/estimates.xlsx`
- **Действие**: Извлечение всех строк из Excel как есть
- **Выход**: `1_extracted/raw_estimates.json`
- **Статус**: ✅ Уже реализован

#### 3.2 Шаг 2: Классификатор (classifier.py)
- **Вход**: `1_extracted/raw_estimates.json`
- **Действие**: Классификация "Работа/Материал" (правила + AI)
- **Выход**: `2_classified/classified_estimates.json`
- **Статус**: ✅ Уже реализован

#### 3.3 Шаг 3: Препаратор (preparer.py) - НОВЫЙ
- **Вход**: `2_classified/classified_estimates.json` + `0_input/directives.json`
- **Действие**: Фильтрация работ, сборка единого JSON + создание блочной системы недель из `timeline_blocks.py`
- **Выход**: `3_prepared/true.json` (включает недельные блоки)
- **Статус**: ❌ Требует создания

### Этап 4: AI-Агенты (Шаги 4-7) 🧠

#### 4.1 Общая Архитектура Агентов
Каждый агент следует единому паттерну:
1. Чтение `true.json` из предыдущего шага
2. Формирование запроса к LLM (данные + промт + директива)
3. Сохранение запроса в `llm_input.json`
4. Вызов LLM API
5. Сохранение ответа в `llm_response.json`
6. Обогащение `true.json`

#### 4.2 Агент 1: Концептуализатор
- **Цель**: Группировка работ по логическим блокам
- **Добавляет**: `group_id` к каждой работе
- **Папка**: `4_conceptualized/`

#### 4.3 Агент 2: Стратег  
- **Цель**: Планирование временных этапов по недельным блокам
- **Добавляет**: `schedule_phases` (массив ID недель, например: [1, 2, 6, 7])
- **Папка**: `5_scheduled/`

#### 4.4 Агент 3: Бухгалтер
- **Цель**: Подсчет объемов при группировке
- **Добавляет**: `consolidated_volume`, `unit_cost`  
- **Папка**: `6_accounted/`

#### 4.5 Агент 4: Прораб
- **Цель**: Распределение людских ресурсов по недельным блокам
- **Добавляет**: `worker_counts` (массив по неделям, например: [10, 10, 12, 9])
- **Папка**: `7_staffed/`

### Этап 5: Финальный Отчет 📄

#### 5.1 Репортер (reporter.py)
- **Вход**: `7_staffed/true.json`
- **Действие**: Генерация Excel календарного плана
- **Выход**: `8_output/schedule.xlsx`

### Этап 6: Главный Пайплайн 🎯

#### 6.1 main_pipeline.py
Координирует выполнение всех шагов:
- Управляет последовательностью вызовов
- Обрабатывает ошибки
- Логирование прогресса
- Возврат результатов в телеграм

## Технические Детали

### Зависимости
```
pandas
python-telegram-bot
python-dotenv
google-generativeai
openpyxl
uuid
logging
```

### API Интеграции
- Telegram Bot API
- Google Gemini API  
- Сметное Дело API (опционально)

### Форматы Данных

#### directives.json
```json
{
  "target_work_count": 15,
  "project_timeline": {
    "start_date": "2024-01-01", 
    "end_date": "2024-06-30"
  },
  "workforce_range": {"min": 10, "max": 20},
  "directives": {
    "conceptualizer": "всю электрику в один блок",
    "strategist": "растяни демонтаж на весь первый месяц", 
    "accountant": "при объединении полов считай только площадь",
    "foreman": "на отделку кинь максимум людей"
  }
}
```

#### true.json (финальный)
```json
{
  "meta": {...},
  "directives": {...},
  "timeline_blocks": [
    {"week_id": 1, "start_date": "2024-01-01", "end_date": "2024-01-07"},
    {"week_id": 2, "start_date": "2024-01-08", "end_date": "2024-01-14"},
    "..."
  ],
  "work_items": [
    {
      "id": "uuid",
      "original_data": {...},
      "group_id": "group_1", 
      "schedule_phases": [1, 2, 6, 7],
      "consolidated_volume": 100.5,
      "worker_counts": [10, 10, 12, 9]
    }
  ]
}
```

## Порядок Реализации

1. **Создание структуры папок** - основа архитектуры
2. **Телеграм-бот** - интерфейс пользователя  
3. **Препаратор** - недостающее звено между классификацией и агентами
4. **AI-агенты** - последовательно, один за другим
5. **Репортер** - финальный Excel
6. **Главный пайплайн** - сборка всего воедино
7. **Тестирование** - на реальных сметах

## Особые Замечания

- Промты пока не создаем - сначала архитектура
- Используем виртуальное окружение
- Все API ключи в .env
- Полное логирование всех этапов
- Инкрементальная разработка с тестированием каждого модуля

## Команды для Разработки

```bash
# Создание виртуального окружения
python -m venv venv
source venv/bin/activate

# Установка зависимостей  
pip install pandas python-telegram-bot python-dotenv google-generativeai openpyxl

# Запуск бота
python main_bot.py

# Тестирование отдельных модулей
python -m src.data_processing.extractor
python -m src.data_processing.classifier  
```

---

*План создан для инкрементальной разработки системы HerZog v3.0*